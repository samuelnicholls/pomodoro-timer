import { FC } from 'react';

const Header: FC = () => {
  return (
    <header className="w-full md:p-6 p-4 bg-gray-800 border-b border-gray-200 text-white text-center">
      <h1>Pomodoro Timer</h1>
    </header>
  );
};

export default Header;
